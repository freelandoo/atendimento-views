# ✅ ESTE É O REPOSITÓRIO CANÔNICO (PRODUÇÃO)

- Conectado ao GitHub: `freelandoo/atendimento-views.git` (branch `master`, sincronizado).
- Deploy: Railway **grateful-nourishment** (produção).
- Estrutura: `backend/` (Node/Express) + `frontend/` (Next.js).
- Migrations 001–013 (bate com o banco de produção).
- Já contém "Banco de Leads unificado + Aquisição".

➡️ **É AQUI que se trabalha e faz deploy.** Renomear esta pasta para `atendimento-views`
(combina com o nome do remote) quando o IDE liberar o lock.

⚠️ Não confundir com `atendimento-views-master`, que é um snapshot ANTIGO/obsoleto.
Ver `_LEGADO-atendimento-views-master/_LEIA-ME-OBSOLETO.md`.
